// import shop from '@/api/shop'
export default { // = methods, should be complex, but never update the state.
  // make the call
  // run setProducts mutation
  // context.commit
  // context.state
  // destructuring javascript with {state, getters, commit} from context
}
